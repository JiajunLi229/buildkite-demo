
steps:
  - command: echo 'Tests'
    label: ':hammer:'
  - wait
  - trigger: dependent-pipeline-deploy
    label: ":rocket:"
    branches: "main"
    async: true
    build:
      commit: "$BUILDKITE_COMMIT"
      branch: "$BUILDKITE_BRANCH"
      message: "$BUILDKITE_MESSAGE"
